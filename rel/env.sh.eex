# Release environment defaults for the official image (SPEC §24.6).
# The web endpoint serves unless the worker role disables it.
if [ "${WEB_SERVER:-true}" != "false" ]; then
  export PHX_SERVER=true
fi
